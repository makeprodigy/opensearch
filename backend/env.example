# Rename this file to `.env` for local development.
PORT=4000
DATABASE_URL="mysql://user:password@host:3306/goodfirstfinder"
JWT_SECRET="replace-with-strong-secret"
GITHUB_TOKEN="github_pat_XXXXXXXXXXXXXXXXXXXXXXXXXXXX"
LOG_LEVEL="info"
CRON_SCHEDULE="*/5 * * * *"
CORS_ORIGIN="http://localhost:5173"

# Repository cleanup configuration
# REPO_TTL_DAYS: Number of days to keep repository data before cleanup (default: 7)
REPO_TTL_DAYS=7
# CLEANUP_INTERVAL_HOURS: How often to run the cleanup job in hours (default: 1)
CLEANUP_INTERVAL_HOURS=1

